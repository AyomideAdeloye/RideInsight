Flask==3.0.3
Flask-WTF==1.2.1
Flask-Limiter==3.8.0
Werkzeug==3.0.4
python-dotenv==1.0.1
requests==2.32.3
bleach==6.1.0
# Renders the Fit Guide prose in content/fit-guides/*.md. Repo-authored content
# only — never point it at user submissions.
Markdown==3.7
gunicorn==22.0.0
